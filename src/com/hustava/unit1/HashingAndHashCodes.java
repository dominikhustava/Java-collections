package com.hustava.unit1;

public class HashingAndHashCodes {

    //hashing:
    //taking (complex) object and reducing it to one value - applying hashing function, and it will return hashcode
    //one way thing, it's not compressing. Same object has to return same value every time
    //Scenario
    //Labeled Drawers, organizing students by name, or patients and storing it according of it
    //hash collisions sometimes can occur
}
